FROM golang:1.21-alpine AS builder
WORKDIR /src
COPY go.mod ./
COPY server ./server
RUN go build -o /out/gotunnel-server ./server

FROM alpine:3.20
WORKDIR /app
COPY --from=builder /out/gotunnel-server /usr/local/bin/gotunnel-server
EXPOSE 6000 6001
ENTRYPOINT ["gotunnel-server"]
